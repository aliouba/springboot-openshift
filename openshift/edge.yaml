apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: spring-boot-app
  name: spring-boot-app
spec:
  host: <name here>.apps.cloud.dakar.io
  tls:
    termination: edge
  to:
    kind: Service
    name: spring-boot-app
